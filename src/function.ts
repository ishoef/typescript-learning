// Function
// arrow Function , normal function

// normal function
function add(num1: number, num2: number): number {
  return num1 + num2;
}
console.log(add(5, 10));

// arrow function
const arrowFunc = (num1: number, num2: number): number => num1 + num2;
console.log(arrowFunc(2, 50));
